# Система рассылок TMA-Подписка

## Обзор

Система рассылок позволяет администраторам отправлять сообщения пользователям Telegram с возможностью сегментации аудитории. Поддерживаются как немедленная отправка, так и планирование на будущее.

## Возможности

### 📯 Типы целевой аудитории

- **Все пользователи** - Отправка всем зарегистрированным пользователям
- **Активные подписки** - Только пользователям с действующими подписками
- **Истекшие подписки** - Пользователям, у которых истек срок подписки
- **Триал пользователи** - Пользователи с активным демо-доступом
- **Конкретный продукт** - Пользователи конкретного продукта
- **Конкретный канал** - Подписчики конкретного канала
- **Кастомный фильтр** - Гибкая фильтрация по различным параметрам

### ⏰ Варианты отправки

- **Немедленная отправка** - Рассылка запускается сразу
- **Запланированная отправка** - Отправка в указанное время
- **Автоматическая обработка** - Фоновые задачи обрабатывают запланированные рассылки

### 📊 Статистика и аналитика

- Отслеживание статуса каждого сообщения
- Детальная статистика по доставке
- Мониторинг ошибок и недоставленных сообщений
- Прогресс отправки в реальном времени

## Структура системы

### 🗄️ Модели данных

#### Broadcast
```typescript
interface Broadcast {
  broadcastId: string        // Уникальный ID рассылки
  title: string             // Название рассылки
  message: string           // Текст сообщения
  targetType: BroadcastTargetType  // Тип целевой аудитории
  status: BroadcastStatus   // Статус рассылки
  scheduledAt?: Date        // Запланированное время отправки
  sentAt?: Date            // Фактическое время отправки
  createdBy: bigint        // ID администратора
  totalRecipients: number  // Общее количество получателей
  sentCount: number        // Количество отправленных сообщений
  failedCount: number      // Количество ошибок
}
```

#### BroadcastMessage
```typescript
interface BroadcastMessage {
  messageId: string         // ID сообщения
  broadcastId: string      // ID рассылки
  userId: bigint           // ID пользователя
  status: MessageStatus    // Статус сообщения
  sentAt?: Date           // Время отправки
  error?: string          // Текст ошибки
  telegramMessageId?: number  // ID сообщения в Telegram
}
```

#### BroadcastFilter
```typescript
interface BroadcastFilter {
  filterId: string     // ID фильтра
  broadcastId: string  // ID рассылки
  filterType: FilterType  // Тип фильтра
  filterValue: string  // Значение фильтра
}
```

### 🔗 API Эндпоинты

#### Управление рассылками
- `GET /api/admin/broadcasts` - Получение списка рассылок
- `POST /api/admin/broadcasts` - Создание новой рассылки
- `GET /api/admin/broadcasts/[id]` - Получение деталей рассылки
- `PUT /api/admin/broadcasts/[id]` - Обновление рассылки
- `DELETE /api/admin/broadcasts/[id]` - Удаление рассылки

#### Отправка и статистика
- `POST /api/admin/broadcasts/[id]/send` - Запуск отправки рассылки
- `GET /api/admin/broadcasts/[id]/stats` - Получение статистики
- `POST /api/admin/broadcasts/preview` - Предпросмотр получателей

#### Фоновые задачи
- `GET /api/cron/scheduled-broadcasts` - Обработка запланированных рассылок

### 🏗️ Компоненты интерфейса

#### BroadcastManagement
Основной компонент админ-панели для управления рассылками:
- Список рассылок с пагинацией
- Фильтрация по статусу
- Создание новых рассылок
- Управление статусами

#### BroadcastPreview
Компонент для предпросмотра получателей:
- Расчет количества получателей
- Демо-список пользователей
- Информация о сегментации

#### BroadcastStats
Компонент статистики:
- Детальная аналитика отправки
- Мониторинг ошибок
- Прогресс в реальном времени

## Настройка и развертывание

### 1. Переменные окружения

Добавьте в `.env` файл:

```env
# Telegram Bot API
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Cron Job Security
CRON_SECRET=your_secret_key_here
```

### 2. Миграция базы данных

```bash
npx prisma migrate dev --name add-broadcast-system
npx prisma generate
```

### 3. Настройка Cron Job

Настройте запуск cron задачи для обработки запланированных рассылок:

```bash
# Пример для cron (каждую минуту)
* * * * * curl -H "Authorization: Bearer $CRON_SECRET" https://your-app.com/api/cron/scheduled-broadcasts
```

### 4. Настройка Telegram Bot

Убедитесь, что бот имеет права на отправку сообщений пользователям:
- Включите режим Privacy Off для бота
- Добавьте команды для управления рассылками (опционально)

## Использование

### 1. Создание рассылки

1. Перейдите в админ-панель → "📢 Рассылки"
2. Нажмите "Новая рассылка"
3. Заполните поля:
   - **Название**: Описательное название для идентификации
   - **Целевая аудитория**: Выберите тип получателей
   - **Сообщение**: Текст для отправки (поддерживается HTML)
   - **Планирование**: Необязательно, укажите время для отложенной отправки

### 2. Предпросмотр получателей

1. После выбора целевой аудитории нажмите "Показать получателей"
2. Система покажет количество и список получателей
3. Убедитесь, что аудитория выбрана корректно

### 3. Отправка

**Немедленная отправка:**
- Нажмите "Создать" (если без планирования)
- Рассылка появится в списке со статусом "Черновик"
- Нажмите иконку отправки 📤

**Запланированная отправка:**
- Укажите время в поле "Запланировать отправку"
- Нажмите "Создать"
- Система автоматически отправит в указанное время

### 4. Мониторинг

1. В списке рассылок отслеживайте статусы
2. Нажмите на иконку статистики 📊 для детальной информации
3. Проверяйте ошибки и скорость доставки

## Статусы рассылок

- **Черновик (DRAFT)**: Рассылка создана, но не отправлена
- **Запланирована (SCHEDULED)**: Рассылка запланирована на будущее
- **Отправляется (SENDING)**: Идет процесс отправки сообщений
- **Завершена (COMPLETED)**: Все сообщения обработаны
- **Ошибка (FAILED)**: Критическая ошибка при отправке
- **Отменена (CANCELLED)**: Рассылка отменена администратором

## Типы фильтров

### PRODUCT_ID
Фильтрация по конкретному продукту:
```json
{
  "filterType": "PRODUCT_ID",
  "filterValue": "product_123"
}
```

### CHANNEL_ID
Фильтрация по конкретному каналу:
```json
{
  "filterType": "CHANNEL_ID",
  "filterValue": "-1001234567890"
}
```

### SUBSCRIPTION_STATUS
Фильтрация по статусу подписки:
```json
{
  "filterType": "SUBSCRIPTION_STATUS",
  "filterValue": "active" // или "expired"
}
```

### REGISTRATION_DATE
Фильтрация по дате регистрации:
```json
{
  "filterType": "REGISTRATION_DATE",
  "filterValue": "{\"from\":\"2024-01-01\",\"to\":\"2024-12-31\"}"
}
```

## Безопасность

### 1. Защита от спама
- Встроенные задержки между отправками
- Пакетная обработка сообщений
- Соблюдение лимитов Telegram API

### 2. Обработка блокировок
- Автоматическое определение заблокированных ботов
- Исключение недоставленных сообщений из повторных отправок

### 3. Аутентификация
- Проверка прав администратора для всех операций
- Секретный ключ для cron задач

## Мониторинг и отладка

### Логи
Система логирует:
- Создание и изменение рассылок
- Статусы отправки сообщений
- Ошибки API Telegram
- Производительность системы

### Метрики
- Скорость отправки (сообщений в минуту)
- Процент доставленных сообщений
- Частота ошибок по типам
- Время обработки рассылок

## Ограничения и рекомендации

### Лимиты Telegram API
- Максимум 30 сообщений в секунду на один бот
- Рекомендуется не более 1 сообщения в секунду для стабильности
- Используйте batch processing для оптимизации

### Рекомендации
- Тестируйте рассылки на небольшой аудитории
- Используйте предпросмотр для проверки сегментации
- Мониторьте статус отправки в реальном времени
- Создавайте резервные копии важных рассылок

## Расширения

### Возможные улучшения
1. **Персонализация**: Подстановка имени пользователя в сообщения
2. **A/B тестирование**: Тестирование разных вариантов сообщений
3. **Шаблоны**: Сохранение часто используемых сообщений
4. **Черный список**: Исключение определенных пользователей
5. **Аналитика**: Графики и детальная статистика
6. **Интерактивные кнопки**: Добавление inline кнопок в сообщения

### Интеграции
- Email-рассылки для пользователей без Telegram
- Push-уведомления в веб-приложении
- SMS-уведомления для важных сообщений
- Вебхуки для внешних систем

---

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли разработчика
2. Убедитесь, что бот имеет права на отправку сообщений
3. Проверьте переменные окружения
4. Очистите кэш и перезагрузите страницу

Для дополнительной информации обращайтесь к документации Telegram Bot API и разработчикам системы.