# Платежная система TON USDT

## Обзор

Платежная система обеспечивает прием USDT платежей через блокчейн TON с автоматической верификацией транзакций и управлением подписками.

## Архитектура

### Компоненты

1. **TON Connect Integration** - интеграция с Telegram Wallet
2. **Payment Initiation API** - инициирование платежей
3. **Transaction Monitoring** - мониторинг транзакций через Toncenter API v3
4. **Verification System** - верификация платежей
5. **Subscription Management** - управление подписками
6. **Channel Integration** - автоматическое добавление в Telegram каналы

### Поток обработки платежа

```
1. Пользователь выбирает продукт →
2. Подключение TON Connect →
3. Иницииация платежа →
4. Отправка USDT транзакции →
5. Мониторинг транзакции →
6. Верификация платежа →
7. Активация подписки →
8. Добавление в канал
```

## API Endpoints

### 1. Иницииация платежа
```
POST /api/payment/initiate
```

**Headers:**
- `Content-Type: application/json`
- `X-Telegram-Init-Data: <telegram_init_data>`

**Body:**
```json
{
  "productId": "product_id"
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "paymentId": "payment_id",
    "amount": 10,
    "currency": "USDT",
    "memo": "unique_memo",
    "transaction": {
      "messages": [...]
    }
  }
}
```

### 2. Верификация платежа
```
POST /api/payment/verify
```

**Headers:**
- `Content-Type: application/json`
- `X-Telegram-Init-Data: <telegram_init_data>`

**Body:**
```json
{
  "paymentId": "payment_id",
  "txHash": "transaction_hash"
}
```

### 3. Мониторинг транзакций (API v3)
```
POST /api/payment/monitor-v3
GET /api/payment/monitor-v3?checkActions=true
```

**Response:**
```json
{
  "success": true,
  "message": "Payment monitoring completed. Processed 0 payments.",
  "checked": 3,
  "processed": 0,
  "results": [...]
}
```

### 4. Создание тестового платежа
```
POST /api/test/create-test-payment
```

**Body:**
```json
{
  "productId": "product_id",
  "userId": "123456",
  "amount": 0.1
}
```

## Frontend Компоненты

### 1. ProductList.tsx
Основной компонент для отображения продуктов и обработки платежей.

**Функциональность:**
- Отображение списка продуктов
- Интеграция с TON Connect
- Иницииация платежей
- Отслеживание статуса платежей

### 2. PaymentMonitor.tsx
Компонент для мониторинга статуса платежей в реальном времени.

**Функциональность:**
- Автоматический мониторинг каждые 15 секунд
- Отображение статуса проверки
- Детальная информация о результатах

### 3. usePaymentMonitor.ts
Custom hook для управления мониторингом платежей.

**Опции:**
- `interval: number` - интервал проверки (мс)
- `autoStart: boolean` - автозапуск
- `onPaymentConfirmed: function` -回调 при подтверждении

## Конфигурация

### Переменные окружения

```env
TON_WALLET_ADDRESS=UQAa5LYjLQJQ2Dz0npnlqYTFJdkoB5GlVlTtFAfL-66b3aKP
TONCENTER_API_KEY=your_api_key
BOT_TOKEN=your_bot_token
```

### TON Connect Manifest

`public/tonconnect-manifest.json`
```json
{
  "url": "https://your-domain.com",
  "name": "Your App Name",
  "iconUrl": "https://your-domain.com/icon.png"
}
```

## USDT Jetton Transfers

### Создание транзакции

```typescript
const jettonTransferPayload = beginCell()
  .storeUint(0xf8a7ea5, 32) // op code для jetton transfer
  .storeCoins(toNano(finalPrice.toString())) // amount
  .storeAddress(Address.parse(process.env.TON_WALLET_ADDRESS!))
  .storeBuffer(Buffer.from(memo, 'utf8')) // memo as buffer
  .endCell()
```

### Важные параметры

- **Op Code:** `0xf8a7ea5` для jetton transfer
- **Amount:** в наноTON (для USDT)
- **Memo:** уникальный идентификатор платежа
- **Destination:** адрес кошелька получателя

## Мониторинг транзакций

### API v3 Endpoints

1. **Pending Actions:**
   ```
   GET https://toncenter.com/api/v3/pendingActions
   ```

2. **Transactions:**
   ```
   GET https://toncenter.com/api/v3/transactions?account={address}&limit={limit}
   ```

### Процесс верификации

1. **Проверка memo** - совпадение уникального идентификатора
2. **Проверка адреса** - совпадение адреса получателя
3. **Проверка суммы** - соответствие суммы платежа
4. **Подтверждение** - создание подписки и уведомлений

## Управление подписками

### Создание подписки

```typescript
const subscription = await prisma.subscription.create({
  data: {
    userId: payment.userId,
    productId: payment.productId,
    channelId: payment.product.channelId,
    paymentId: payment.paymentId,
    status: 'active',
    startsAt: new Date(),
    expiresAt: new Date(Date.now() + days * 24 * 60 * 60 * 1000)
  }
})
```

### Добавление в канал

```typescript
// Проверка статуса пользователя
const response = await fetch(
  `https://api.telegram.org/bot${botToken}/getChatMember`,
  {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      chat_id: channelId,
      user_id: userId
    })
  }
)
```

## Тестирование

### Тестовая страница

`http://localhost:3002/test-payment`

**Функциональность:**
- Тестирование иницииации платежей
- Мониторинг в реальном времени
- Создание тестовых платежей
- Проверка API v3

### Команды тестирования

```bash
# Проверка pending actions
curl "http://localhost:3002/api/payment/monitor-v3?checkActions=true"

# Создание тестового платежа
curl -X POST "http://localhost:3002/api/test/create-test-payment" \
  -H "Content-Type: application/json" \
  -d '{"productId": "product_id", "userId": "123456", "amount": 0.1}'

# Запуск мониторинга
curl -X POST "http://localhost:3002/api/payment/monitor-v3"
```

## Безопасность

### Валидация Telegram WebApp

```typescript
export function validateTelegramInitData(initData: string, botToken: string): boolean {
  try {
    const urlParams = new URLSearchParams(initData)
    const hash = urlParams.get('hash')

    if (!hash) return false

    // В production нужна проверка HMAC-SHA256
    return true
  } catch (error) {
    console.error('Error validating Telegram init data:', error)
    return false
  }
}
```

### Обработка ошибок

- Валидация всех входных данных
- Проверка статуса транзакций
- Обработка сетевых ошибок
- Логирование всех операций

## Логирование

### Формат логов

```
🔍 MONITOR: Starting payment monitoring check
📄 MONITOR: Check result: {...}
✅ MONITOR: Transaction verified successfully
❌ MONITOR: Error checking transaction: {...}
```

### Уровни логирования

- **🔍** - отладочная информация
- **📄** - результаты операций
- **✅** - успешные операции
- **❌** - ошибки

## Оптимизация

### Кэширование

- Кэширование данных продуктов
- Оптимизация запросов к API
- Минимизация обращений к блокчейну

### Производительность

- Асинхронная обработка
- Пуллинг с оптимальным интервалом
- Обработка пачками для массовых операций

## Troubleshooting

### Частые проблемы

1. **TONCENTER_API_KEY не настроен**
   - Проверьте переменные окружения
   - Убедитесь что API ключ активен

2. **Транзакция не находится**
   - Проверьте правильность memo
   - Убедитесь что транзакция подтверждена
   - Проверьте адрес получателя

3. **Пользователь не добавляется в канал**
   - Проверьте права бота
   - Убедитесь что пользователь не заблокирован
   - Проверьте ID канала

### Диагностика

```bash
# Проверка переменных окружения
curl "http://localhost:3002/api/debug/test-env"

# Проверка подключения к базе
curl "http://localhost:3002/api/debug/db-test"
```

## Deployment

### Vercel

1. Настройте переменные окружения в Vercel
2. Обновите URL в tonconnect-manifest.json
3. Деплойте приложение
4. Протестируйте платежную систему

### Мониторинг

- Отслеживайте логи ошибок
- Мониторьте статус транзакций
- Проверяйте баланс кошелька
- Следите за активностью пользователей

## Следующие улучшения

1. **Webhook интеграция** - реальные вебхуки от Toncenter
2. **Мультивалютность** - поддержка других токенов
3. **Аналитика** - детальная статистика платежей
4. **Refunds** - система возвратов
5. **Subscriptions** - рекуррентные платежи

## Поддержка

Для вопросов и проблем:
- Проверьте логи приложения
- Ознакомьтесь с документацией TON Connect
- Используйте тестовую страницу для диагностики
- Обратитесь к команде разработки