# Руководство по демо-доступу

## 🎯 Обзор функциональности

Система демо-доступа позволяет пользователям получить бесплатный доступ к каналам на ограниченный период для оценки контента.

## ✅ Реализованные возможности

### 1. Запрос демо-доступа
- **Мини-приложение:** Пользователь нажимает кнопку "Демо X дней" на карточке продукта
- **Строгая проверка:** Система проверяет, что у пользователя:
  - Нет активной подписки на этот продукт
  - Нет ранее использованного демо-доступа на этот продукт
  - Нет активного демо-доступа на этот продукт
- **Блокировка повторных демо:** Каждый пользователь может получить демо для продукта только один раз
- **Создание записи:** В БД создается запись в таблице `DemoAccess`

### 2. Автоматическое добавление в канал
- **Создание ссылки-приглашения:** Bot API создает уникальную invite ссылку со сроком действия 24 часа
- **Отправка сообщения:** Пользователь получает красивое сообщение со ссылкой и кнопками
- **Кнопки в сообщении:**
  - 🚀 Открыть канал (прямая ссылка на вступление)
  - 📱 Открыть Mini App (для управления подписками)

### 3. Отслеживание времени демо-периода
- **Cron Job:** Ежедневная проверка демо-доступов в 3:00 AM UTC
- **Автоматическая деактивация:** Демо-доступ автоматически деактивируется после истечения срока
- **Интеграция с подписками:** Если у пользователя появляется платная подписка, демо деактивируется

### 4. Автоматическое удаление пользователя
- **Проверка по окончании:** Cron Job проверяет истекшие демо-доступы
- **Удаление из канала:** Пользователь удаляется из канала через Ban/UnBan
- **Уведомление:** Отправляется сообщение об окончании демо с кнопкой для покупки

### 5. Административное управление
- **Панель администратора:** Полный контроль демо-доступов через `/admin`
- **Действия с демо-доступами:**
  - Выдача демо-доступа любому пользователю
  - Продление демо-периода
  - Отзыв доступа (деактивация)
  - **Удаление навсегда** (полное удаление записи и исключение из канала)

## 🛠 Техническая реализация

### API Эндпоинты

#### 1. `/api/demo` (POST/GET)
- **POST:** Запрос демо-доступа для продукта
- **GET:** Получение демо-доступов текущего пользователя
- **Валидация:** Проверка Telegram WebApp initData

#### 2. `/api/cron/check-all` (POST)
- **Авторизация:** Защита через `CRON_SECRET`
- **Проверка:** Поиск истекших демо-доступов и подписок
- **Обработка:** Удаление пользователей и отправка уведомлений

#### 3. `/api/admin/demo/*` (администраторские эндпоинты)
- **GET /access:** Получение всех демо-доступов
- **POST /grant:** Выдача демо-доступа пользователю
- **POST /extend/{id}:** Продление демо-доступа
- **DELETE /revoke/{id}:** Отзыв демо-доступа (деактивация)
- **DELETE /delete/{id}:** Полное удаление демо-доступа
- **Валидация:** Проверка прав администратора

#### 4. `/api/test-demo` (GET)
- **Тестирование:** Проверка статистики демо-доступов
- **Диагностика:** Отладочная информация о состоянии системы

#### 5. `/api/test-delete-demo` (GET/POST)
- **GET:** Получение списка демо-доступов для тестирования
- **POST:** Проверка существования демо-доступа перед удалением

#### 6. `/api/products/with-demo-status` (GET)
- **Функциональность:** Получение продуктов со статусом демо для пользователя
- **Защита от повторных демо:** `allowDemo: false` если демо уже использован
- **Информация о статусе:** `demoStatus.hasUsed`, `demoStatus.isActive`

#### 7. `/api/test-demo-prevention` (GET/POST)
- **GET:** Тестирование логики предотвращения повторных демо
- **POST:** Проверка конкретного пользователя-продукта на возможность демо

### База данных

#### Таблица `DemoAccess`
```sql
- id: BigInt (Primary Key)
- userId: BigInt (Foreign Key -> User.telegramId)
- productId: String (Foreign Key -> Product.productId)
- startedAt: DateTime
- expiresAt: DateTime
- isActive: Boolean
```

### Cron Jobs

#### Vercel Configuration (`vercel.json`)
```json
{
  "crons": [
    {
      "path": "/api/cron/check-all",
      "schedule": "0 3 * * *"  // Ежедневно в 3:00 AM UTC
    }
  ]
}
```

## 📱 Пользовательский сценарий

### 1. Запрос демо-доступа
1. Пользователь открывает Mini App
2. Выбирает продукт с поддержкой демо
3. Нажимает "Демо X дней" (доступно только если демо никогда не использовался)
4. Система проверяет отсутствие предыдущих демо-доступов
5. Получает уведомление об успешной активации
6. Получает сообщение в Telegram со ссылкой на канал

### 2. Использование демо-доступа
1. Пользователь переходит по ссылке из сообщения
2. Вступает в канал
3. Имеет доступ к контенту в течение демо-периода
4. Кнопка "Демо" становится неактивной ("Демо использовано")

### 3. Окончание демо-периода
1. Cron Job обнаруживает истекший демо-доступ
2. Пользователь удаляется из канала
3. Отправляется уведомление об окончании демо
4. Кнопка демо остается неактивной ("Демо использовано")

### 4. Попытка повторного демо (ЗАБЛОКИРОВАНО)
1. Пользователь видит серую неактивную кнопку "Демо использовано"
2. При попытке нажать - ничего не происходит
3. Информационный блок сообщает, что демо уже использован
4. Предлагается купить полную подписку

## 🔧 Настройка и развертывание

### Переменные окружения
```
BOT_TOKEN=               # Токен Telegram бота
APP_URL=                # URL приложения
CRON_SECRET=            # Секрет для защиты cron jobs (опционально)
```

### Настройка бота
1. Добавить бота во все каналы с правами:
   - Invite users via link
   - Ban users
   - Manage invite links

### Тестирование
1. **API тест:** `GET /api/test-demo`
2. **Ручной тест:** Запрос демо через Mini App
3. **Cron тест:** Ручной запуск `/api/cron/check-demo-access`

## 🎨 Сообщения пользователям

### Активация демо
```
🎉 Демо-доступ активирован!

📦 Продукт: [Название продукта]
📅 Ваш демо-период начался!

🔗 Ссылка для входа в канал:
[invite_link]

Нажмите на ссылку выше, чтобы присоединиться к каналу.

⚠️ Внимание: Ссылка действительна 24 часа.
```

### Окончание демо
```
📋 Ваш демо-доступ завершен

📦 Продукт: [Название продукта]
📢 Канал: [Название канала]

Ваш бесплатный демо-период закончился.
Вы больше не имеете доступа к закрытому контенту.

🛍️ Чтобы продолжить доступ:
Нажмите кнопку ниже и выберите подходящий тариф.
```

## ⚡ Производительность и оптимизация

### Эффективность
- **Выборка данных:** Оптимизированные запросы к БД с `include`
- **Batch обработка:** Групповая обработка демо-доступов в cron job
- **Кэширование:** Базовые операции без кэширования для актуальности

### Масштабирование
- **Vercel Functions:** Автоматическое масштабирование
- **Postgres:** Высокопроизводительная БД
- **Telegram API:** Ограничения по запросам учтены

## 🔒 Безопасность

### Защита данных
- **Валидация Telegram:** Проверка подписи WebApp данных
- **BigInt handling:** Безопасная работа с большими числами
- **Cron защита:** Авторизация через `CRON_SECRET`

### Предотвращение злоупотреблений
- **Уникальность:** Один демо-доступ на пользователя-продукт
- **Временные ограничения:** Ограниченный срок действия
- **Проверка конфликтов:** Нельзя иметь демо и подписку одновременно

## 🚀 Мониторинг и отладка

### Логирование
- **Детальные логи:** Все операции демо-доступа логируются
- **Статистика:** Сбор метрик использования
- **Ошибки:** Подробное логирование исключений

### Диагностика
- **Health check:** `/api/test-demo` endpoint
- **Cron статус:** Логи выполнения фоновых задач
- **Bot API:** Проверка ответов от Telegram

## 📈 Метрики успеха

- **Конверсия в демо:** % пользователей запрашивающих демо
- **Активация демо:** % успешно вступивших в каналы
- **Конверсия в подписку:** % демо-пользователей купивших подписку
- **Время до покупки:** Средний срок от демо до покупки