# ⏰ Настройка Vercel Cron для автоматической проверки подписок

## 🎯 Назначение

Автоматическая ежедневная проверка подписок с:
- Уведомлениями о продлении (7, 3, 1 день до истечения)
- Автоматическим отключением истекших подписок
- Удалением пользователей из каналов

---

## 🔧 Требуемые Environment Variables

В Vercel Dashboard → Project Settings → Environment Variables:

### Обязательные переменные:
```bash
BOT_TOKEN=1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  # Токен Telegram бота
APP_URL=https://tma-subscription.vercel.app                # URL приложения
DATABASE_URL=postgresql://user:password@host:port/database # База данных
ADMIN_TELEGRAM_ID=123456789                                # ID администратора

# Для защиты cron job (рекомендуется)
CRON_SECRET=your_secret_key_here                           # Секрет для cron
```

### Опциональные переменные:
```bash
NEXT_PUBLIC_APP_URL=https://tma-subscription.vercel.app    # Публичный URL
NODE_ENV=production                                        # Окружение
```

---

## ⚙️ Конфигурация Cron

### Текущая конфигурация (vercel.json):
```json
{
  "crons": [
    {
      "path": "/api/cron/check-subscriptions",
      "schedule": "0 2 * * *"
    }
  ]
}
```

**Расписание:** `0 2 * * *`
- Каждую ночь в 02:00 UTC
- Около 05:00 по Москве (летом) / 04:00 (зимой)

### Изменение расписания:
```json
{
  "crons": [
    {
      "path": "/api/cron/check-subscriptions",
      "schedule": "0 12 * * *"  // В 12:00 UTC (15:00 по Москве)
    }
  ]
}
```

**Примеры расписаний:**
- `0 */6 * * *` - каждые 6 часов
- `0 8,14,20 * * *` - в 8, 14, 20 UTC
- `*/30 * * * *` - каждые 30 минут (только для тестирования)

---

## 🚀 Деплоймент и активация

### 1. Развертывание с конфигурацией:
```bash
# Коммит изменений
git add vercel.json
git commit -m "Add Vercel Cron configuration for daily subscription checks"
git push origin main

# Автоматический деплой в Vercel
```

### 2. Настройка в Vercel Dashboard:
1. Откройте проект в Vercel
2. Перейдите в Settings → Environment Variables
3. Добавьте все необходимые переменные окружения
4. Перейдите в Settings → Functions → Cron Jobs
5. Проверьте, что cron job появился в списке

### 3. Проверка работы:
```bash
# Ручной запуск для тестирования
curl -X GET "https://tma-subscription.vercel.app/api/cron/check-subscriptions" \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

---

## 🧪 Тестирование Cron Job

### 1. Тестовый запуск:
```bash
# Без авторизации (если CRON_SECRET не настроен)
curl -X GET "https://tma-subscription.vercel.app/api/cron/check-subscriptions"

# С авторизацией (рекомендуется)
curl -X GET "https://tma-subscription.vercel.app/api/cron/check-subscriptions" \
  -H "Authorization: Bearer your_secret_key_here"
```

### 2. Проверка логов:
1. Vercel Dashboard → Functions → Logs
2. Фильтр по пути: `/api/cron/check-subscriptions`
3. Проверка времени последнего запуска

### 3. Мониторинг результатов:
**Успешный ответ:**
```json
{
  "success": true,
  "message": "Processed 0 expired subscriptions",
  "data": {
    "processed": 0,
    "errors": 0,
    "total": 0
  }
}
```

**Ответ с уведомлениями:**
```json
{
  "success": true,
  "message": "Processed 0 expired subscriptions",
  "data": {
    "processed": 0,
    "errors": 0,
    "total": 0
  }
}
```
(В логах будет информация об отправленных уведомлениях)

---

## 📊 Мониторинг и отладка

### 1. Vercel Logs:
- **Function Logs**: Логи выполнения cron job
- **Build Logs**: Логи развертывания
- **Real-time Logs**: Логи в реальном времени

### 2. Telegram Bot Logs:
```bash
# Проверка логов webhook
grep "subscription expiry check" vercel.log
grep "Sent.*day reminder" vercel.log
grep "Processed subscription" vercel.log
```

### 3. Database Monitoring:
```sql
-- Проверка активных подписок
SELECT COUNT(*) FROM subscription
WHERE status = 'active' AND expiresAt > NOW();

-- Проверка истекших подписок
SELECT COUNT(*) FROM subscription
WHERE status = 'expired';

-- Проверка подписок, истекающих в ближайшие 7 дней
SELECT COUNT(*) FROM subscription
WHERE status = 'active'
AND expiresAt BETWEEN NOW() AND NOW() + INTERVAL '7 days';
```

---

## ⚠️ Возможные проблемы и решения

### Проблема: Cron не запускается
**Причины:**
1. Неправильная конфигурация в vercel.json
2. Отсутствие необходимых environment variables
3. Ошибки в коде endpoint

**Решение:**
```bash
# Проверка конфигурации
cat vercel.json | jq '.crons'

# Проверка переменных окружения
vercel env ls

# Тестовый запуск endpoint
curl -X GET "https://tma-subscription.vercel.app/api/cron/check-subscriptions"
```

### Проблема: Ошибка авторизации
**Причина:** Настроен CRON_SECRET, но не передан в запросе

**Решение:**
```bash
# Получить секрет из Vercel
vercel env ls

# Добавить в webhook запрос (Vercel делает это автоматически)
# или временно убрать проверку в коде для тестирования
```

### Проблема: Нет уведомлений
**Причины:**
1. Не настроен BOT_TOKEN
2. Пользователи заблокировали бота
3. Ошибки в Telegram API

**Решение:**
```bash
# Проверить токен бота
curl -X GET "https://api.telegram.org/botYOUR_BOT_TOKEN/getMe"

# Проверить логи на ошибки
grep "Error sending" vercel.log
grep "Failed to answer callback query" vercel.log
```

### Проблема: База данных недоступна
**Причина:** Проблемы с DATABASE_URL или подключением

**Решение:**
```bash
# Проверить connection string
vercel env pull .env.local

# Тест подключения к БД
npx prisma db pull
```

---

## 📈 Оптимизация производительности

### 1. Таймауты и ограничения:
- **Максимальное время выполнения**: 30 секунд
- **Максимальное количество подписок**: 1000 за один запуск
- **Задержка между отправками**: 100ms (для избежания лимитов Telegram)

### 2. Эффективные запросы:
```typescript
// Использование индексов в базе данных
CREATE INDEX idx_subscription_expires_at ON subscription(expiresAt);
CREATE INDEX idx_subscription_status ON subscription(status);

// Batch обработка для большого количества подписок
const batchSize = 50;
for (let i = 0; i < expiredSubscriptions.length; i += batchSize) {
  const batch = expiredSubscriptions.slice(i, i + batchSize);
  await processBatch(batch);
}
```

### 3. Мониторинг производительности:
```typescript
// Добавить метрики времени выполнения
console.time('subscription-check');
// ... выполнение кода ...
console.timeEnd('subscription-check');
```

---

## 🔄 Резервное копирование и восстановление

### 1. Резервное копирование данных:
```bash
# Ежедневный бэкап базы данных
pg_dump $DATABASE_URL > backup_$(date +%Y%m%d).sql
```

### 2. Восстановление после сбоя:
```bash
# В случае сбоя cron job
curl -X POST "https://api.telegram.org/botYOUR_TOKEN/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": ADMIN_TELEGRAM_ID,
    "text": "⚠️ Cron job failed. Manual intervention required."
  }'
```

---

## 📋 Чек-лист настройки

### ✅ Перед деплоем:
- [ ] `vercel.json` содержит cron конфигурацию
- [ ] Все environment variables настроены в Vercel
- [ ] Бот имеет права администратора в каналах
- [ ] База данных доступна и содержит данные

### ✅ После деплоя:
- [ ] Cron job появился в Vercel Dashboard
- [ ] Тестовый запуск прошел успешно
- [ ] Логи показывают правильную работу
- [ ] Уведомления приходят пользователям

### ✅ Регулярная проверка:
- [ ] Ежедневная проверка логов
- [ ] Еженедельная проверка статистики
- [ ] Ежемесячная проверка производительности

---

**🎉 После настройки Vercel Cron система будет полностью автоматизирована!**

Теперь все проверки подписок и уведомления будут работать без ручного вмешательства.