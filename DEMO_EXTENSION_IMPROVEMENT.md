# Улучшение функции продления демо-доступа

## Описание изменений
Заменили фиксированную кнопку "+7 дней" на гибкую систему продления с календарем для выбора точной даты окончания демо-доступа.

## Новая функциональность

### 🗓️ **Календарь для выбора даты**
- Пользователь может выбрать любую дату в будущем
- Минимальная дата - текущий день
- Умная валидация формата даты

### ⚡ **Быстрые опции продления**
- +7 дней
- +14 дней
- +30 дней
- Кнопки для быстрого выбора популярных периодов

### 📱 **Улучшенный UX**
- Информационное окно с данными о демо-доступе
- Отображение текущей даты окончания
- Валидация на стороне клиента и сервера

## Технические изменения

### API эндпоинт: `/api/admin/demo/extend/[id]`
**Изменения:**
- Вместо `additionalDays` принимает `newExpiresAt`
- Валидация формата даты
- Проверка, что дата в будущем

```typescript
// БЫЛО:
{ additionalDays: number }

// СТАЛО:
{ newExpiresAt: string } // формат YYYY-MM-DD
```

### Фронтенд: `DemoManagement.tsx`
**Новые компоненты:**
- Модальное окно для выбора даты продления
- Календарь с валидацией
- Кнопки быстрого выбора

**Новые функции:**
- `openExtendModal(demoId)` - открытие модального окна
- `extendDemoAccess()` - продление с выбранной датой

## Инструкция по использованию

### Для администратора:
1. В списке демо-доступов нажмите кнопку "Продлить"
2. В открывшемся окне увидите информацию о демо-доступе
3. Выберите новую дату окончания:
   - Через календарь ИЛИ
   - Через кнопки быстрого выбора (+7, +14, +30 дней)
4. Нажмите "Продлить" для подтверждения

### Преимущества нового подхода:
- ✅ **Гибкость**: Выберите любую дату вместо фиксированных 7 дней
- ✅ **Удобство**: Быстрые кнопки для популярных периодов
- ✅ **Информативность**: Видна текущая и новая дата окончания
- ✅ **Валидация**: Нельзя выбрать прошедшую дату

## Обработка ошибок

### Client-side:
- Проверка выбора даты перед отправкой
- Блокировка кнопки "Продлить" если дата не выбрана

### Server-side:
- Валидация формата даты
- Проверка, что дата в будущем
- Проверка существования демо-доступа

## Коды ответов API:
- `200`: Успешное продление
- `400`: Невалидная дата или дата в прошлом
- `404`: Демо-доступ не найден
- `401`: Неавторизованный доступ
- `500`: Внутренняя ошибка сервера

## Пример использования

```javascript
// Запрос на продление:
POST /api/admin/demo/extend/demo123
{
  "newExpiresAt": "2025-12-25"
}

// Ответ:
{
  "demoAccess": {
    "id": "demo123",
    "userId": "123456789",
    "productId": "prod456",
    "expiresAt": "2025-12-25T00:00:00.000Z",
    "isActive": true
  }
}
```

## Обратная совместимость
Старые вызовы API с `additionalDays` больше не поддерживаются. Необходимо обновить все клиенты для использования нового формата с `newExpiresAt`.

## Тестирование
1. Проверить работу календаря
2. Проверить кнопки быстрого выбора
3. Проверить валидацию (невалидные даты, прошедшие даты)
4. Проверить обновление списка после продления
5. Проверить обработку ошибок сервера