# Установка и развертывание

## 🚀 Быстрый старт

Развертывание TMA-Подписка занимает 15-30 минут с нуля.

### 📋 Требования

#### 🔧 Обязательные
- **Аккаунт Vercel** с тарифом Pro (для Postgres и cron jobs)
- **GitHub репозиторий** (для автоматического деплоя)
- **Telegram бот** (создать у [@BotFather](https://t.me/BotFather))
- **TON кошелек** для приема USDT платежей

#### 💡 Рекомендуемые
- **Базовые знания** JavaScript/TypeScript
- **Понимание** Telegram Bot API
- **Опыт работы** с Vercel или подобными платформами

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

#### 🔄 Клонирование проекта
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd tma-subscription

# Установите зависимости
npm install

# Сгенерируйте Prisma клиент
npx prisma generate

# (Опционально) Запустите локально для тестов
npm run dev
```

#### 🔗 Подключение к Vercel
```bash
# Установите Vercel CLI
npm i -g vercel

# Авторизуйтесь в Vercel
vercel login

# Свяжите проект с Vercel
vercel link
```

### Шаг 2: Создание Telegram бота

#### 🤖 Создание бота через BotFather
1. **Найдите** [@BotFather](https://t.me/BotFather) в Telegram
2. **Отправьте команду** `/newbot`
3. **Введите имя** бота (например, "TMA Subscription Bot")
4. **Введите username** бота (например, `tma_subscription_bot`)
5. **Сохраните** полученный токен бота

#### ⚙️ Настройка бота
```bash
# Установите команды бота
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setMyCommands" \
  -H "Content-Type: application/json" \
  -d '{
    "commands": [
      {"command": "start", "description": "🚀 Начать работу"},
      {"command": "subscriptions", "description": "📱 Мои подписки"},
      {"command": "help", "description": "❓ Помощь"}
    ]
  }'
```

#### 🔒 Установка вебхука
После развертывания на Vercel:
```bash
# Замените на ваш домен
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -d "url=https://your-app.vercel.app/api/webhook/telegram"
```

### Шаг 3: Настройка базы данных

#### 🗄️ Создание Postgres базы на Vercel
1. **Откройте** [Vercel Dashboard](https://vercel.com/dashboard)
2. **Перейдите** в ваш проект
3. **Перейдите** в Storage → Create Database
4. **Выберите** PostgreSQL и создайте базу
5. **Сохраните** подключение данные

#### 📊 Настройка схемы
```bash
# Push схемы в базу данных
npx prisma db push

# (Опционально) Заполните начальными данными
npx prisma db seed
```

#### 🔍 Просмотр базы данных
```bash
# Откройте Prisma Studio для управления
npx prisma studio
```

### Шаг 4: Настройка переменных окружения

#### 🔧 Создание .env файла
Создайте файл `.env.local` в корне проекта:

```bash
# ===========================================
# Telegram Bot Configuration
# ===========================================
BOT_TOKEN=your_telegram_bot_token_here
BOT_SECRET=your_bot_webhook_secret_here

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_URL=postgresql://user:password@host:port/database
POSTGRES_PRISMA_URL=postgresql://user:password@host:port/database?pgbouncer=true
POSTGRES_URL_NON_POOLING=postgresql://user:password@host:port/database

# ===========================================
# TON Configuration
# ===========================================
TONCENTER_API_KEY=your_toncenter_api_key_here
TON_WALLET_ADDRESS=your_usdt_wallet_address_here

# ===========================================
# Admin Configuration
# ===========================================
ADMIN_TELEGRAM_ID=your_admin_telegram_id_here

# ===========================================
# Application Configuration
# ===========================================
APP_URL=https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ===========================================
# Security Configuration
# ===========================================
NEXTAUTH_SECRET=your_nextauth_secret_here
JWT_SECRET=your_jwt_secret_here

# ===========================================
# Optional: NOWPayments Configuration
# ===========================================
NOWPAYMENTS_API_KEY=your_nowpayments_api_key_here
NOWPAYMENTS_IPN_SECRET=your_ipn_secret_here

# ===========================================
# Optional: Monitoring Configuration
# ===========================================
VERCEL_ANALYTICS_ID=your_analytics_id_here
SENTRY_DSN=your_sentry_dsn_here
```

#### 🔑 Получение необходимых ключей

##### TON Center API Key
1. **Перейдите** на [toncenter.com](https://toncenter.com/)
2. **Зарегистрируйтесь** и получите API ключ
3. **Сохраните** ключ в переменные окружения

##### Адрес TON кошелька
1. **Создайте** TON кошелек (Tonkeeper, MyTonWallet)
2. **Получите** USDT адрес кошелька
3. **Сохраните** адрес в переменные окружения

### Шаг 5: Развертывание на Vercel

#### 🚀 Первое развертывание
```bash
# Разверните проект в продакшн
vercel --prod

# Или через GitHub (рекомендуется)
git add .
git commit -m "Initial deployment"
git push origin main
```

#### ⚙️ Настройка Cron Jobs
После первого развертывания:

1. **Откройте** Vercel Dashboard → ваш проект
2. **Перейдите** в Settings → Functions → Cron Jobs
3. **Добавьте** следующие cron jobs:

```bash
# Проверка подписок каждые 6 часов
Check Subscriptions: 0 */6 * * *

# Проверка демо-доступа ежедневно
Check Demo Access: 0 0 * * *

# Полная проверка системы ежедневно
System Check: 0 2 * * *
```

4. **Настройте** URL для каждого cron job:
   - `/api/cron/check-subscriptions`
   - `/api/cron/check-demo-access`
   - `/api/cron/check-all`

### Шаг 6: Инициализация системы

#### 👨‍💼 Создание администратора
```sql
-- Добавьте себя в таблицу администраторов
INSERT INTO admins (telegram_id, role, permissions)
VALUES (YOUR_TELEGRAM_ID, 'super_admin', '["all"]');
```

Или через API:
```bash
curl -X POST "https://your-app.vercel.app/api/admin/create" \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": YOUR_TELEGRAM_ID,
    "role": "super_admin",
    "permissions": ["all"]
  }'
```

#### 📺 Добавление каналов
1. **Добавьте бота** в ваши Telegram каналы
2. **Предоставьте права** на добавление участников
3. **Создайте каналы** в системе через админ-панель

#### 🛍️ Создание продуктов
1. **Откройте** админ-панель: `https://your-app.vercel.app/admin`
2. **Перейдите** в раздел "Управление продуктами"
3. **Создайте** первые продукты с ценами и периодами

---

## 🎯 Тестирование развертывания

### 🧪 Проверка базового функционала

#### ✅ API Health Check
```bash
# Проверка работоспособности API
curl https://your-app.vercel.app/api/health

# Проверка подключения к базе
curl https://your-app.vercel.app/api/db/check

# Проверка TON API
curl https://your-app.vercel.app/api/ton/check
```

#### 🤖 Тестирование бота
1. **Найдите** вашего бота в Telegram
2. **Отправьте** команду `/start`
3. **Проверьте** корректность ответа
4. **Попробуйте** открыть Mini App

#### 💳 Тестирование платежей
1. **Откройте** Mini App
2. **Выберите** тестовый продукт
3. **Попробуйте** подключить TON кошелек
4. **Проверьте** создание транзакции (не отправляйте)

### 🔧 Поиск проблем

#### 🚨 Частые ошибки и решения

##### Ошибка: "Invalid Telegram WebApp data"
**Причина**: Неправильный BOT_TOKEN или BOT_SECRET
**Решение**: Проверьте и обновите переменные окружения

##### Ошибка: "Database connection failed"
**Причина**: Проблемы с POSTGRES_URL
**Решение**: Проверьте строку подключения в Vercel Storage

##### Ошибка: "TON API limit exceeded"
**Причина**: Превышен лимит запросов к Toncenter
**Решение**: Получите платный API ключ или добавьте задержки

##### Ошибка: "Cron job not working"
**Причина**: Неправильная настройка cron в Vercel
**Решение**: Проверьте путь и расписание в настройках Vercel

---

## 📊 Мониторинг и обслуживание

### 📈 Vercel Monitoring

#### 🔍 Метрики для отслеживания
- **Function Response Time** (должен быть < 3 секунд)
- **Error Rate** (должен быть < 1%)
- **Database Query Time** (должен быть < 100ms)
- **Success Rate** (должен быть > 99%)

#### 📋 Логи и ошибки
```bash
# Просмотр логов через Vercel CLI
vercel logs

# Просмотр логов конкретной функции
vercel logs --filter="/api/payment/initiate"
```

### 🔧 Регулярное обслуживание

#### 🗄️ База данных
```bash
# Обновление схемы при изменениях
npx prisma db push

# Создание бэкапов (через Vercel Storage UI)
# Рекомендуется: ежедневные бэкапы
```

#### 🤖 Telegram бот
```bash
# Проверка статуса вебхука
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo

# Сброс вебхука при проблемах
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook
```

#### 💰 TON кошелек
- **Регулярно проверяйте** баланс кошелька
- **Выводите средства** на холодный кошелек
- **Мониторьте** транзакции через TON Explorer

---

## 🎯 Чек-лист развертывания

### ✅ Проверка перед запуском

- [ ] **Vercel проект создан** и связан с GitHub
- [ ] **Postgres база данных** создана и настроена
- [ ] **Переменные окружения** заполнены корректно
- [ ] **Telegram бот** создан и настроен
- [ ] **TON кошелек** подготовлен для приемов
- [ ] **Cron jobs** настроены в Vercel
- [ ] **Администратор** добавлен в систему
- [ ] **Каналы** добавлены в систему
- [ ] **Продукты** созданы с ценами
- [ ] **API тесты** пройдены успешно
- [ ] **Бот работает** в Telegram
- [ ] **Mini App открывается** без ошибок
- [ ] **Платежи инициализируются** корректно

### 🚀 После запуска

- [ ] **Мониторинг** настроен и работает
- [ ] **Бэкапы** базы данных созданы
- [ ] **Уведомления** об ошибках настроены
- [ ] **Документация** для пользователей готова
- [ ] **Тестовая покупка** выполнена успешно

---

## 🆘 Поддержка и помощь

### 📚 Полезные ресурсы
- [Vercel Documentation](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [TON Connect Documentation](https://docs.ton.org/develop/dapps/ton-connect)
- [Prisma Documentation](https://www.prisma.io/docs)

### 🎯 Следующие шаги
После успешного развертывания:
1. **Изучите** [конфигурацию системы](./configuration.md)
2. **Познакомьтесь** с [API справочником](./api-reference.md)
3. **Настройте** [безопасность](./security.md)

**Поздравляем! 🎉 Ваша система подписок готова к работе!**

[⚙️ Конфигурация системы](./configuration.md) | [🔒 Безопасность](./security.md)